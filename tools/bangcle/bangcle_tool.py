import os

CURR_DIR = os.path.dirname(__file__)

#PLATFORM = "macos" if sys.platform.index("darwin") >=0 else "linux" if sys.platform.index("linux") >=0 else "windows"

PACKEE_CONFIG = "%s/packeeconf"%(CURR_DIR)